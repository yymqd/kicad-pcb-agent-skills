# 贡献指南

感谢你愿意一起完善这套 KiCad PCB 设计工作流！

## 核心约定：管线归属（务必阅读）

本工作流的核心是**三条管线的阶段切换**（详见 README）：

- **管线 A**：kicad-cli + pcbnew —— 验证/导出（DRC、Gerber、钻孔、3D、SVG）
- **管线 B**：MCP-KiCad (mixelpixx) —— 设计（原理图、布局、单层布线、JLCPCB）
- **管线 C**：MCP-kcaa (paul356) —— 高级设计（多层 PNS 布线、电路模式识别）

**任何改动必须声明管线归属**，并在 PR 描述中说明：

1. 属于哪个阶段（设计/验证/导出/仿真/审计）
2. 走哪条管线（A/B/C）
3. 如果改变了「阶段 → 管线」的映射，必须说明理由

> ⚠️ 不要破坏阶段切换约定。这是本工作流最容易被人改乱的地方。

## 如何贡献

### 新案例沉淀（最欢迎）
实测过的设计/修复经验，格式参考现有 references/：
- 环境与版本（KiCad 版本、OS、MCP 版本）
- 问题现象 + 根因
- 解决方案（代码/命令）
- 验证结果

### 修复与增强
- Fork → 修改 → PR
- 每个 SKILL.md 的改动请保持 frontmatter 完整（name/description/trigger）
- 新增脚本放到对应 skill 的 `scripts/` 目录

### 报告问题
用 ISSUE 模板，分类：
- 管线故障（标注 A/B/C）
- 工具版本适配（KiCad 7/8/9/10 差异是最大坑）
- 文档错误

## 代码规范
- Python：兼容 Python 3.8+，避免依赖过重的库
- 路径：一律用占位符（`<KICAD_DIR>` 等），不硬编码个人路径
- 中文字符路径陷阱：脚本默认 ASCII 路径

## 提交信息
```
[管线A] 修复 DRC 检查遗漏项
[管线B] 新增 JLCPCB 元件搜索示例
[案例] 新项目：XX 驱动板（Ø14mm 双面）
```
